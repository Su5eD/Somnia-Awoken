buildscript {
    repositories {
        maven { url = 'https://files.minecraftforge.net/maven' }
        jcenter()
        mavenCentral()
    }
    dependencies {
        classpath group: 'net.minecraftforge.gradle', name: 'ForgeGradle', version: '3.+', changing: true
    }
}
apply plugin: 'net.minecraftforge.gradle'
apply plugin: 'eclipse'
apply plugin: 'maven-publish'

def VERSION_CLASSIFIER = VERSION_CLASSIFIER != "" ? "-"+VERSION_CLASSIFIER : ""
def BUILD_NUMBER = "$System.env.GITHUB_RUN_NUMBER" != "null" ? "-$System.env.GITHUB_RUN_NUMBER" : ""
version = VERSION_MC + "-" + VERSION_MAJOR + "." + VERSION_MINOR + (Integer.parseInt(VERSION_REVISION) > 0 ? "."+VERSION_REVISION : "") + BUILD_NUMBER + VERSION_CLASSIFIER
group = 'mods.su5ed'

sourceCompatibility = targetCompatibility = compileJava.sourceCompatibility = compileJava.targetCompatibility = '1.8' // Need this here so eclipse task generates correctly.

minecraft {

    mappings channel: 'snapshot', version: '20201028-1.16.3'
    
    accessTransformer = file('src/main/resources/META-INF/somnia_at.cfg')

    runs {
        client {
            workingDirectory project.file('run')

            property 'forge.logging.markers', 'SCAN,REGISTRIES,REGISTRYDUMP,COREMODLOG'

            property 'forge.logging.console.level', 'debug'

            mods {
                somnia {
                    source sourceSets.main
                }
            }
        }

        server {
            workingDirectory project.file('run')

            property 'forge.logging.markers', 'SCAN,REGISTRIES,REGISTRYDUMP,COREMODLOG'

            property 'forge.logging.console.level', 'debug'

            mods {
                somnia {
                    source sourceSets.main
                }
            }
        }

        data {
            workingDirectory project.file('run')

            property 'forge.logging.markers', 'SCAN,REGISTRIES,REGISTRYDUMP'

            property 'forge.logging.console.level', 'debug'

            args '--mod', 'examplemod', '--all', '--output', file('src/generated/resources/')

            mods {
                somnia {
                    source sourceSets.main
                }
            }
        }
    }
}

dependencies {
    minecraft 'net.minecraftforge:forge:1.16.4-35.1.28'
}

processResources {
    from(sourceSets.main.resources) {
        include "META-INF/mods.toml"
        expand 'version': project.version
    }
}

jar {
    manifest {
        attributes([
            "Specification-Title": "Somnnia Awoken",
            "Specification-Vendor": "Su5eD",
            "Specification-Version": "1",
            "Implementation-Title": project.name,
            "Implementation-Version": "${project.getVersion()}",
            "Implementation-Vendor" :"Su5eD",
            "Implementation-Timestamp": new Date().format("yyyy-MM-dd'T'HH:mm:ssZ")
        ])
    }
}

jar.finalizedBy('reobfJar')